# 簡易部署說明

## 專案型態

本專案為純靜態網站，由 HTML、CSS、JavaScript 與本機圖片素材組成，不需要安裝套件，也沒有建置指令。部署時只要讓網站根目錄對應到專案根目錄即可。

網站入口為 `index.html`，其餘頁面為：

- `products.html`
- `journal.html`
- `article.html`

## 本機預覽

請在專案根目錄執行：

```powershell
py -m http.server 8000
```

接著開啟 `http://localhost:8000`。預覽完成後，可在終端機按 `Ctrl+C` 停止伺服器。

## 部署前檢查

- 確認四個 HTML 頁面都能正常開啟。
- 確認頁面導覽、輪播、行動版選單與搜尋功能可正常操作。
- 確認 `css/`、`js/` 與 `assets/` 目錄完整，且圖片沒有遺失。
- 確認檔案以 UTF-8 編碼儲存，避免中文顯示異常。

## 部署方式

### Netlify 手動部署

1. 登入 Netlify，開啟手動部署頁面。
2. 將整個專案資料夾拖曳到部署區域。
3. 等待部署完成後，開啟 Netlify 提供的網址確認網站。

此專案不需要填寫建置指令，發佈目錄為專案根目錄。

### GitHub Pages

1. 將專案推送到 GitHub 儲存庫。
2. 進入儲存庫的 `Settings` → `Pages`。
3. 在來源設定中選擇要部署的分支，資料夾選擇根目錄 `/ (root)`。
4. 儲存設定，等待 GitHub Pages 產生網站網址。

## 部署後確認

請至少檢查首頁、商品頁、生活提案頁與文章頁，並確認桌面版及行動版的版面、圖片與互動功能皆正常。
